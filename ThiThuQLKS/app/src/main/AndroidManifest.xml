<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="lananh.ptit.thithuqlks">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".admin.ListPhongActivity"></activity>
        <activity android:name=".admin.SuaPhongActivity" />
        <activity android:name=".client.ChiTietPhongActivity" />
        <activity android:name=".admin.ThemPhongActivity" />
        <activity android:name=".client.ClientHomeActivity" />
        <activity android:name=".admin.AdminHomeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>